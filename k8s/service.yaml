apiVersion: v1
kind: Service
metadata:
  name: claude-api-wrapper
spec:
  type: LoadBalancer
  selector:
    app: claude-api-wrapper
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
